% Generated by hand to accompany R/data-binned.R
\name{bin_samples}
\alias{bin_samples}
\alias{bucket_summary}
\alias{oob_performance}
\docType{data}
\title{Binned optimisation artifacts}
\description{
Outputs from \code{run_binned_pipeline()} summarising the optimisation sweeps and post-processing used in the bucket selection visuals.
}
\format{A tibble combining sweeps across requested bin counts with columns:
\describe{
  \item{N}{Number of bins in the sweep.}
  \item{Range_Min}{Lower bound of the bin's input interval.}
  \item{Range_Max}{Upper bound of the bin's input interval.}
  \item{Magic}{Magic constant expressed as a hexadecimal string.}
  \item{Avg_Relative_Error}{Average relative error over the interval.}
  \item{Max_Relative_Error}{Worst-case relative error over the interval.}
}}
\usage{data(bin_samples)
data(bucket_summary)
data(oob_performance)}
\details{
Additional columns emitted by \code{frsrr::frsr_bin()} are preserved.\cr
\code{bucket_summary} adds derived metrics such as \code{Depth}, \code{Variance}, and \code{Rarity}.\cr
\code{oob_performance} augments \code{bin_samples} with \code{OOB_Error} measurements.
}
\source{\file{data-raw/pipelines/binned.R}}
\seealso{\code{run_binned_pipeline()} for regeneration guidance and \code{\link{prep_bucket_selection}} for transformation helpers.}
\keyword{datasets}
